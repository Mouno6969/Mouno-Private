REACT_APP_API_URL=https://insured-headlines-challenging-confident.trycloudflare.com
# Keep ESLint running but don't fail the build on warnings
# (run `eslint` as a separate CI step for real enforcement)
CI=false
